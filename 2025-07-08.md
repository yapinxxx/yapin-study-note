<<<<<<< HEAD
# [Melding data, systems, and society](https://news.mit.edu/2025/data-systems-and-society-0610)
> A new book from Professor Munther Dahleh details the creation of a unique kind of transdisciplinary center, uniting many specialties through a common need for data science.
## Summary
1. About MIT program **Institute for Data, Systems and Society (IDSS)**
2. Books: &ldquo;  test &rdquo; “
=======
Sure—let’s do a short, progressive quiz. Pick one or two problems to solve (or explain), and I’ll give you feedback and tips:

1. **Basics & Syntax**
    
    - Write a function `Reverse(s []int) []int` that returns the slice reversed, _in place_ if possible.
		- 
		```
		length:=len(s)
		for i,v:=range s{
			if i>length-1-i{
				return s
			}
			s[i],s[length-1-i] = s[length-1-i],s[i]
		}
		```   

    - What’s the zero value of a `map[string]int`? How do you safely add an entry to it?
	    1. nil
	    2. use `if _,ok:=map[string]int;ok`
        
2. **Types & Interfaces**
    
    - Define an interface `Stringer` (with one method) and implement it on a custom struct.
		- 
			```
			type Stringer interface{
				DoMethod()
			}
			
			type MyStruct struct{
			}
			
			func (m MyStruct) DoMethod(){}
			func main(){
				var myIf Stringer
				myIf = MyStruct{}
				myIf.DoMethod()
			}
			```
        
    - Explain why you might return an interface type instead of a concrete struct.
	    - It could make code more flexible
	    - If I want to add a new struct, I only need to confirm it correspond all methods of the interface 
        
3. **Error Handling**
    
    - Show how to wrap an error with additional context using Go 1.13’s `errors` package.
        - `fmt.Errorf()`
    - What’s the difference between `panic` and returning an error? When would you choose one over the other?
        - If the error need to be force interrupt then use panic, if we could tolerate the error will be auto fix, then use error
4. **Concurrency**
    
    - Write a function that launches N goroutines to compute square of numbers from 1 to 100, collects results via a channel, and returns the sum.
	    - 
			```
			sum:=0
			for i:=1;i<=100;i++{
				myCh:=make(chan int)
				go func(myCh chan, side int){
					myCh <- i*i
				}(myCh,i)
				sum += <- myCh
			}
			```
    - Describe a race condition you might hit with a shared counter, and how you’d fix it.
	    - use `sync` package
	    - I remember their have `mutex` to do lock, unlock, rwlock to help solve this type problem.
        
5. **Advanced**
    
    - Sketch a simple pipeline: read lines from a file, transform them (e.g. uppercase), and write to another file—using channels.
	    - use the io package to read line by line and output to another files, need to remember `defer` the file
        
    - How does Go’s garbage collector work at a high level? What trade-offs does it make?
	    - I forgot
>>>>>>> 4bf8aba (Update Content)
